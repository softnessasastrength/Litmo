import { Text } from 'react-native';
import { PocScreen } from '../../components/PocScreen';

export default function ConsentSetupScreen() {
  return <PocScreen title="Consent Setup"><Text>Body-zone, pressure, and hard-stop preferences will be configured here.</Text></PocScreen>;
}

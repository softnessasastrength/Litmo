import { Text } from 'react-native';
import { PocScreen } from '../../components/PocScreen';

export default function ActiveSessionScreen() {
  return <PocScreen title="Active Session"><Text>Timer, grounding prompts, and the always-available Soft Signal will live here.</Text></PocScreen>;
}

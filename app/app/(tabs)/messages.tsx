import { Body, Eyebrow, Screen, Title } from "../../components/ui";

export default function MessagesTabScreen() {
  return (
    <Screen>
      <Eyebrow>MESSAGES</Eyebrow>
      <Title>Not built yet.</Title>
      <Body muted>
        Messaging isn&rsquo;t part of the prototype yet. When it exists, this is
        where it will live. Nothing here is being logged or connected to a real
        inbox.
      </Body>
    </Screen>
  );
}
